$(function() {
   $("#left-nav").mmenu({
      // options
   }, {
      // configuration
      offCanvas: {
         pageNodetype: "section",
         pageSelector: "#olark-wrapper"
      }
   });
});
